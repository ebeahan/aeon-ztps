This will build ova files with AEON-ZTPS fully installed.

Requirements:
packer
ovftool (must be placed in PATH)
aos-aeonztps-addons in same directory as this aeon-ztps directory

To build OVA:
sh build_ova.sh