auto lo
iface lo inet loopback

{% for intf in interfaces %}
auto {{ intf.name }}
iface {{ intf.name }} inet static
    address {{ intf.ip.address }}
    netmask {{ intf.ip.netmask }}
    gateway {{ intf.ip.gateway }}
{% endfor %}
